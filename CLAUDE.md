# CLAUDE.md — AddBuy+C 開発ルール

このファイルは Claude Code がこのプロジェクトで作業する際のルールを定義します。

## プロジェクト構成

- **フレームワーク**: Next.js (App Router)
- **データベース**: Supabase
- **ホスティング**: Vercel
- **リポジトリ**: GitHub

## 禁止事項

- Vercel の設定・環境変数を勝手に変更しない
- Supabase のプロジェクト設定・RLSポリシーを勝手に変更しない
- `.env.local` や環境変数を無断で書き換えない
- `service_role` キーは絶対に使用しない（anon key のみ使用可）
- `main` ブランチへ直接 push しない

## 作業フロー

1. **変更前に必ず説明する**: 対象ファイルと変更方針をユーザーに伝えてから実装に入る
2. **作業は小さく分ける**: 1回のタスクで変更するファイルや機能を最小限に絞る
3. **実装後に lint を実行する**: `npm run lint` を実行してエラーがないことを確認する
4. **コミット前に確認する**: `git status` を実行し、変更点をユーザーに要約してから commit する
5. **ブランチ → PR → main マージ**: 機能追加・修正はブランチを切り、PR を通して main にマージする

## 本番確認

- 本番URL: **https://addbuyc.com**
- Vercel が生成する `*.vercel.app` の個別デプロイURLは本番確認に使わない
